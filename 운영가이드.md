# implant-quiz-ad 운영 가이드

작성일: 2026-08-31

---

## 1. 이 프로젝트가 뭔지

- 폴더: `C:\Users\mmh12\implant-quiz-ad`
- 배포 주소: `event4tk.knockknockdental.com`
- Vercel 프로젝트명: `implant-quiz-ad` (팀: `munmihye9-2323s-projects`)
- 구조: 정적 페이지(`index.html`) + 서버 기능 1개(`api/submit.js`)
- 데이터 저장: 구글 시트 (Apps Script를 통해 폼 제출 데이터가 시트로 들어감)

---

## 2. 지금까지 적용한 스팸 방지 기능

폼 제출 시 아래 3가지를 순서대로 검사합니다.

### ① 이름 검증 (브라우저에서 즉시 체크)
- 자음/모음 낱자만 있는 이름 차단 (예: ㅋㅋㅋ, ㅁㄴㅇㄹ)
- 같은 글자 반복 차단 (예: 가가가)
- 욕설 단어 포함 시 차단

### ② 전화번호 검증 (브라우저에서 즉시 체크)
- 010으로 시작하는 정상 형식인지 확인
- 동일 숫자 반복(예: 01000000000), 연속 숫자(예: 01012345678) 같은 장난 번호 차단

### ③ IP 기준 5분 재제출 차단 (서버에서 확실하게 체크)
- 구조: 브라우저 → `/api/submit` (Vercel 서버 함수) → 구글 Apps Script → 구글 시트
- `api/submit.js`가 요청자의 실제 IP를 확인해서, Upstash Redis(무료 DB)에 "이 IP가 5분 이내 제출했는지" 기록/조회
- 5분 이내 재요청이면 429 에러로 차단하고 구글 시트까지 데이터가 넘어가지 않음
- 이 방식 덕분에 시크릿모드/캐시삭제로는 못 뚫는 진짜 IP 기준 제한이 적용됨 (참고: 브라우저 저장소(localStorage) 방식은 이런 우회가 가능해서 서버 방식으로 처리함)

### 연동된 외부 서비스
- **Upstash for Redis** (Vercel Storage 통합, Free 플랜) — 리소스명 `upstash-kv-bronze-brush`
- 프로젝트 환경변수: `UPSTASH_REDIS_KV_REST_API_URL`, `UPSTASH_REDIS_KV_REST_API_TOKEN`

---

## 3. 여러 이벤트로 확장하는 계획

### 왜 필요한가
지금은 코드에 우리 구글시트 주소가 그대로 박혀있어서, 이 상태로 다른 사람이 복사해 쓰면 실수로 우리 시트로 데이터가 들어옴. 이걸 팀원이 각자 안전하게 복사해서 쓸 수 있는 "템플릿" 구조로 정리하는 게 목표.

### 구조
```
implant-landing-template   ← 원본 (틀), 직접 이벤트에는 안 씀
implant-quiz-event5xx      ← 독립 레포 #1 (원본과 완전히 분리된 별개 레포)
implant-quiz-event6xx      ← 독립 레포 #2
```
- "동위(독립) 레포" 방식 사용 — 하위 레포/서브모듈 아님
- GitHub의 **"Use this template"** 기능으로 원본을 복사하면, 원본과 연결고리 없는 완전히 새 레포가 생성됨

### 네이밍 규칙 (통일 추천)
`레포 이름 = Vercel 프로젝트 이름 = 도메인 이름`
예: 레포 `implant-quiz-event5xx` → Vercel 프로젝트 `implant-quiz-event5xx` → 도메인 `event5xx.knockknockdental.com`

### Vercel 계정 운영 방식
- 개인 계정에 흩어지지 않게 **회사 공용 Vercel Team** 생성 권장
- 장점: 도메인/권한/환경변수를 한곳에서 관리
- 비용: Vercel Team(Pro 플랜)은 팀원 1명당 매달 비용 발생 (개인 Hobby는 무료) — 팀원 수만큼 고정비 증가하는 점 감안 필요
- (진행 여부 미정 — 회사 계정으로 만들지 결정 필요)

### 새 이벤트 만들 때 순서 (팀원용)
1. 템플릿 레포에서 "Use this template"으로 새 레포 생성 (이름은 네이밍 규칙대로)
2. 자기 이벤트용 구글 시트 새로 생성 + Apps Script 웹앱으로 배포해서 전송주소(exec URL) 발급
3. 회사 Vercel Team 안에 그 레포로 새 프로젝트 생성 (Import)
4. Vercel 환경변수에 `APPS_SCRIPT_URL` (자기 시트 주소) 등록
5. Upstash for Redis 통합 추가 (Custom Prefix는 `UPSTASH_REDIS`로 통일 — 코드가 이 이름을 그대로 참조하기 때문)
6. 배포 → 도메인 연결

### 이후 문구/가격 수정 방법 (개발 지식 없어도 가능)
- Vercel 프로젝트를 깃허브 레포와 "연결"해두면, 깃허브 웹사이트에서 파일을 직접 열어 수정하고 저장(커밋)하는 것만으로 자동 재배포됨
- 컴퓨터에 별도 프로그램 설치나 터미널 사용 불필요

---

## 4. 아직 결정/처리 안 된 것 (보류 중)

- [ ] `api/submit.js`에 하드코딩된 구글 Apps Script 주소를 환경변수(`APPS_SCRIPT_URL`)로 분리 (템플릿화 전 필수 — 안 하면 복사해도 계속 우리 시트로 전송됨)
- [ ] 템플릿 레포 GitHub Public/Private 여부 결정 (Public이면 위 주소 분리가 특히 중요)
- [ ] 회사 공용 Vercel Team 생성 여부 결정
- [ ] 템플릿 레포용 README(사용법 설명서) 작성
- [ ] 기존 Apps Script 코드(.gs)는 구글 쪽에만 있고 이 레포에는 없음 — 템플릿에 포함하려면 별도로 코드를 가져와야 함

---

## 5. 참고 — 오늘 실수로 생긴 것

구글시트 "44TK1" 옆에 **"기타"라는 테스트용 탭**이 생겼습니다. IP 제한 기능을 테스트하면서 실수로 잘못된 탭 이름으로 데이터를 보내서 생긴 것으로, 실제 서비스 데이터와는 무관합니다. 확인 후 삭제하셔도 됩니다.
