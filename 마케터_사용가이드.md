# 마케터용 사용 가이드 — 구글시트 연결/탭 변경

이 문서는 개발 지식이 없어도 따라 할 수 있게 정리했습니다.

---

## 먼저 알아야 할 것: 이 페이지는 이렇게 동작합니다

```
방문자가 폼 작성 → 이 페이지(index.html)가 구글시트로 데이터 전송 → 구글시트의 특정 "탭"에 기록
```

여기서 "탭"이란, 하나의 구글시트 파일 안에 있는 여러 개의 하위 시트를 말합니다. 화면 아래쪽에 `44TK1`, `45HID1`, `46MT1` 처럼 여러 개가 나란히 있는 그 탭들입니다.

**어떤 탭으로 데이터가 들어갈지는 코드 안에 미리 정해져 있습니다.**

---

## 진행 방식 (권장) — 마케터는 "탭 이름"만 정해서 전달

새 이벤트 랜딩이 필요하면, 마케터가 직접 코드를 고치기보다 아래 순서로 진행하는 걸 권장합니다.

### 1. 마케터가 하는 일 — 원하는 탭 이름(인덱스) 정해서 전달
예: "이번 건은 `47EVENT` 탭으로 해주세요"

전달할 때 같이 알려주면 좋은 정보:
- 원하는 탭 이름 (예: `47EVENT`)
- 사용할 도메인 (예: `event7xx.knockknockdental.com`)
- 페이지 문구/가격 등 내용에 변경이 있다면 그 내용

### 2. 개발 담당자가 하는 일 — 설정을 마쳐서 전달
- 구글시트에 그 이름으로 탭 생성
- 코드의 `'탭': '값'` 부분을 요청받은 이름으로 변경
- 배포까지 완료해서 접속 가능한 링크 형태로 마케터에게 전달

### 3. 마케터가 하는 일 — 받은 링크에서 테스트
전달받은 링크에서 실제로 폼을 한 번 제출해보고 확인합니다.

**테스트 체크리스트**
- [ ] 이름 칸에 **"테스트_지워주세요"** 처럼 눈에 띄게 입력해서 제출
- [ ] 구글시트를 열어서, 요청했던 탭(`47EVENT`)에 방금 그 데이터가 정상적으로 들어왔는지 확인
- [ ] 한글이 깨지지 않고 제대로 보이는지 확인
- [ ] 확인 끝나면 테스트로 넣은 행은 삭제
- [ ] 이상 없으면 실제 광고 집행 시작

문제가 있으면(엉뚱한 탭에 들어갔거나, 글자가 깨져서 들어왔거나) 바로 개발 담당자에게 전달 — 대부분 탭 이름이 정확히 일치하지 않아서 생기는 문제입니다.

---

## (참고) 마케터가 직접 코드를 고쳐야 하는 경우

담당자를 거치지 않고 직접 바꿔야 한다면:

1. 구글시트에 새 탭 만들기 (이름은 오타·띄어쓰기·대소문자까지 정확히 기억)
2. `index.html`을 열어 `Ctrl+F`로 **`탭`** 검색
3. 아래 줄을 찾아서 값만 교체
   ```js
   '탭': '44TK1',
   ```
   ↓
   ```js
   '탭': '47EVENT',
   ```
4. 저장 후 배포, 위 "테스트 체크리스트"로 확인

**⚠️ 탭 이름이 실제 시트와 정확히 일치해야 하는 이유:** 이름이 안 맞으면 구글이 자동으로 "기타"라는 새 탭을 만들어버리고, 그 안에 한글 데이터가 깨진 채로 들어갑니다 (실제로 한 번 발생했던 문제).

---

## 완전히 새로운 구글시트(계정/파일 자체를 새로) 쓰고 싶을 때

이건 마케터 혼자 하기 어려운 작업(Apps Script 배포)이 포함되어 있어서 **개발 담당자에게 요청**하세요. 담당자가 참고할 코드는 `apps-script/Code.gs`에 있습니다 — 새 구글시트의 "확장 프로그램 > Apps Script"에 그 코드를 붙여넣고 웹 앱으로 배포하면 됩니다. 배포 후 나온 주소를 `index.html`의 `SCRIPT_URL`에 넣으면 연결이 끝납니다.

이 작업이 끝나면, 이후 탭 추가/변경은 위 "진행 방식(권장)" 그대로 마케터가 요청만 하면 됩니다.

---

## 요약

| 하고 싶은 것 | 누가 하나 | 어디를 바꾸나 |
|---|---|---|
| 같은 시트, 탭만 새로 (권장 방식) | 마케터는 요청만, 개발자가 설정 | `'탭': '값'` 부분 |
| 같은 시트, 탭만 새로 (직접) | 마케터 직접 가능 | `'탭': '값'` 부분 |
| 시트 자체를 새 걸로 | 개발자에게 요청 (`apps-script/Code.gs` 참고) | `SCRIPT_URL` 값 |
